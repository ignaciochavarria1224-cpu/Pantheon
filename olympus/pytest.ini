[pytest]
# Run pytest from the olympus/ directory. pythonpath = . makes the top-level
# `core` and `config` packages importable as `from core.x import y`, matching
# the module layout the ported spine uses.
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
