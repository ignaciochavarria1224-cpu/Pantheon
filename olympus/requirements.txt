# Olympus dependencies (Python 3.11+; verified on 3.14.3). Pinned for reproducibility.
alpaca-py==0.43.4      # Alpaca market data + paper trading
pandas==3.0.3          # data handling / strategy math
APScheduler==3.11.2    # the engine clock
fastapi==0.136.3       # read-only status API (for the Pantheon view later)
uvicorn==0.49.0        # ASGI server for FastAPI
python-dotenv==1.2.2   # load secrets from .env
pytest==9.0.3          # tests (the Article II suite especially)
