# Atlas

The shared memory: a words shelf (Obsidian vault — journals & finished writing), a
numbers shelf (a local database — finances & their daily history), and a light search
layer so Apollo can read across both. Holds Apollo's own conversation history too.
Olympus is walled off and never feeds Atlas.

*Planned.*
